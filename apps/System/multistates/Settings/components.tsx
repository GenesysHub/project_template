import { observer } from '@legendapp/state/react';

export const Menu = (props: any) => {
  return <></>;
};

const Settings = observer((props) => {
  return <>Settings... work in progress</>;
});

export default Settings;
